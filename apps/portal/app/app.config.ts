export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      neutral: 'slate'
    },
    pageHeader: {
      slots: {
        root: 'relative border-b border-default p-4'
      }
    }
  }
})
